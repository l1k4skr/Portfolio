---

---

<section id="contact" class="py-12 md:py-24 lg:py-32">
    <div class="container mx-auto px-4 md:px-6">
        <div class="flex flex-col items-center justify-center space-y-8 text-center">
            <div class="space-y-4">
                <h2 class="text-4xl font-bold sm:text-5xl">
                    Ponte en Contacto
                </h2>
                <p class="max-w-2xl text-lg text-gray-700">
                    Siempre estoy emocionado de discutir nuevos proyectos y oportunidades. No dudes en ponerte en contacto conmigo utilizando el formulario a continuación.
                </p>
            </div>
            <div class="w-full max-w-md mx-auto space-y-6">
                <form class="flex flex-col gap-4">
                    <input
                        class="h-12 px-4 py-2 w-full rounded-md border border-gray-300 bg-white placeholder-gray-400 text-base focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
                        type="text"
                        placeholder="Tu Nombre"
                        name="nombre"
                        required
                    />
                    <input
                        class="h-12 px-4 py-2 w-full rounded-md border border-gray-300 bg-white placeholder-gray-400 text-base focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
                        type="email"
                        placeholder="Tu Email"
                        name="email"
                        required
                    />
                    <textarea
                        class="h-36 px-4 py-2 w-full rounded-md border border-gray-300 bg-white placeholder-gray-400 text-base focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
                        placeholder="Tu Mensaje"
                        name="message"
                        required
                    ></textarea>
                    <button
                        class="h-12 px-6 py-2 w-full rounded-md bg-indigo-600 text-white text-base font-medium hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-opacity-50"
                        type="submit"
                    >
                        Enviar Mensaje
                    </button>
                    <a id="noDisplay"></a>
                </form>
            </div>
        </div>
    </div>
</section>
<script>
    const $form = document.querySelector('form');
    const $button = document.querySelector('#noDisplay');

    $form.addEventListener('submit', async (e) => {
        e.preventDefault();
        const formData = new FormData($form);
        console.log(formData.get('nombre'));
        $button.setAttribute('href', `mailto:andres.antonio2000@hotmail.com?subject=Contacto%20desde%20mi%20portafolio&body=Nombre:%20${formData.get('nombre')}%0D%0AEmail:%20${formData.get('email')}%0D%0AMensaje:%20${formData.get('message')}`);
        $button.click();
});
</script>

<style>
    #noDisplay {
        opacity: 0;
    }
    input, textarea {
        transition: border-color 0.15s ease-in-out, box-shadow 0.15s ease-in-out;
    }
    input:focus, textarea:focus {
        border-color: #2563EB;
        box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.25);
    }
    button {
        transition: background-color 0.15s ease-in-out;
    }
    button:hover {
        background-color: #2563EB;
    }
</style>